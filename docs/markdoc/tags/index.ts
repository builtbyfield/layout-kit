import { column } from "./column.markdoc";
import { columnsContainer } from "./columns-container.markdoc";
import { grid } from "./grid.markdoc";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  column,
  "columns-container": columnsContainer,
  grid,
};
